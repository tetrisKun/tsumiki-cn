---
description: 执行TDD的Red阶段。创建失败的测试用例,明确定义需要实现的功能。
allowed-tools: Read, Glob, Grep, Task, Write, TodoWrite, Edit
argument-hint: [需求名称] [TASK-ID]
---
执行TDD的Red阶段。创建失败的测试用例,明确定义需要实现的功能。

# context

输出目录="./docs/implements"
功能名={{feature_name}}
任务ID={{task_id}}
需求名称={{requirement_name}}
目标测试用例={{test_case_name}}
测试用例添加目标数=10个以上
可靠性评估=[]
需求定义文件=./docs/implements/{需求名称}/{{task_id}}/{feature_name}-requirements.md
测试用例定义文件=./docs/implements/{需求名称}/{{task_id}}/{feature_name}-testcases.md
Red阶段文件=./docs/implements/{需求名称}/{{task_id}}/{feature_name}-red-phase.md
备忘录文件=./docs/implements/{需求名称}/{{task_id}}/{feature_name}-memo.md
任务笔记文件=./docs/implements/{需求名称}/{{task_id}}/note.md

# step

- 如果有 $ARGUMENTS,则设置为目标测试用例名称;如果没有,则以所有测试用例为目标
- 向用户声明 context 的内容
- 执行 step2

## step2

执行开发上下文的准备:

**1. 确认现有的实现文档**
- 确认 `./docs/implements/{需求名称}/{{task_id}}/` 目录内的所有MD文件
- 优先读取以下文件:
  - `note.md` - 任务笔记(技术栈、开发规则、相关实现)
  - `{feature_name}-requirements.md` - 需求定义
  - `{feature_name}-testcases.md` - 测试用例定义
  - `{feature_name}-red-phase.md` - 现有的Red阶段记录
  - `{feature_name}-memo.md` - 开发历史备忘录
  - 其他相关MD文件
- 从这些文件了解现有的实现状况、设计判断、注意事项

**2. 读取附加规则**
- 如果存在 `AGENTS.md` 文件则读取
- 如果存在 `./docs/rule` 目录则读取
- 如果存在 `./docs/rule/tdd` 目录则读取
- 如果存在 `./docs/rule/tdd/red` 目录则读取
- 读取各目录内的所有文件,作为附加规则应用

**3. 读取技术栈定义**
- 如果存在 `./docs/tech-stack.md` 则读取
- 如果不存在则从 `CLAUDE.md` 读取技术栈部分
- 如果都不存在则使用 `.claude/commands/tech-stack.md` 的默认定义

**4. 读取任务笔记**
- 如果在现有文档确认中尚未读取 `./docs/implements/{需求名称}/{{task_id}}/note.md`
- 如果不存在:
  - 使用 @task 执行 `/tsumiki:tdd-tasknote {需求名称} {{task_id}}` 命令生成笔记
  - 读取生成的笔记文件
- 笔记包含技术栈、开发规则、相关实现、设计文档、注意事项

**5. 使用 @agent-symbol-searcher 搜索测试实现相关信息,并读取找到的文件**
- 根据读取的技术栈定义识别测试框架
- **UI任务的情况**: 优先确认E2E测试框架(Playwright等)的设置和示例
- 搜索现有的测试文件或测试函数,使用Read工具读取相应文件
- 识别测试设置或mock的使用模式,使用Read工具读取相关文件
- **E2E测试设置确认**: 使用Read工具读取playwright.config.js、cypress.config.js等配置文件

**6. 直接读取相关的外部文件**
- 根据需要读取相关的设计文档或任务文件
- 项目整体的设计文档、架构文档等

读取完成后,执行 step3

## step3

- 用 context 的信息填充 <red_phase_template> 的内容,创建测试代码
  - 利用读取的上下文信息(任务笔记、附加规则等)
  - 在每个测试用例中记录可靠性级别(🔵🟡🔴)
  - 如果指定了目标测试用例名称,则仅实现该测试用例
  - 如果未指定,则从未实现的测试用例中选择10个以上进行实现
  - 使用 Write 工具保存到测试文件
  - 使用 Bash 工具执行测试,确认失败

- 根据质量判定标准评估创建的测试代码:
  - 测试执行: 可执行且已确认失败
  - 期望值: 明确具体
  - 断言: 适当
  - 实现方针: 明确
  - 可靠性级别(🔵🟡🔴的分布)

- 向用户显示质量判定结果
- 执行 step4

## step4: 文档更新

- **创建/更新Red-phase文件**:
  - 将测试代码和设计内容保存到 {{Red阶段文件}}
  - 如果存在现有文件则追加
  - 记录以下内容:
    - 创建的测试用例列表
    - 测试代码全文
    - 预期的失败内容
    - Green阶段需要实现的内容

- **创建/更新备忘录文件**:
  - 在 {{备忘录文件}} 中创建或追加Red阶段的内容
  - 如果存在现有备忘录文件,则更新Red阶段部分
  - 如果备忘录文件不存在则新建(遵循TDD备忘录文件格式)
  - 记录以下内容:
    - 创建日期时间
    - 测试用例概要
    - 测试代码
    - 预期的失败
    - 下一阶段的要求事项

- 执行 step5

## step5: TODO更新和下一步

- 使用 TodoWrite 工具更新 TODO 状态
  - 将当前TODO"Red阶段(创建失败测试)"标记为"completed"
  - 在TODO内容中反映失败测试创建阶段的完成
  - 在TODO内容中记录质量判定结果
  - 将下一阶段"Green阶段(最小实现)"添加到TODO

- 显示下一步: "推荐的下一步: 使用 `/tsumiki:tdd-green` 开始Green阶段(最小实现)。"

# rules

## 文件名规则

### 输出文件的路径格式
- 测试文件: 根据项目技术栈的适当目录
  - 例: `tests/unit/{feature_name}.test.js`
  - 例: `tests/e2e/{feature_name}.spec.js`
- 备忘录文件: `docs/implements/{需求名称}/{task_id}/{feature_name}-memo.md`
- Red阶段记录: `docs/implements/{需求名称}/{task_id}/{feature_name}-red-phase.md`

### 文件名命名规则
- 将功能名转换为简洁的英语
- 使用短横线命名法(kebab-case)
- 控制在50个字符左右
- 例:
  - "用户认证功能" → "user-auth"
  - "数据导出功能" → "data-export"
  - "密码重置功能" → "password-reset"

## 测试用例添加目标数

**测试用例添加目标数**: 10个以上(如果可用测试用例少于10个则全部添加)

请从未实现的测试用例中选择10个以上的测试用例进行实现。如果可用测试用例少于10个,则将所有可用测试用例作为实现对象。
如果测试用例已经实现,则从测试用例定义中添加测试。
请添加提高需求覆盖率和功能覆盖率的测试用例
NEVER 以500行为目标分割测试用例的最大行数

## 可靠性级别指示

在创建测试代码时,请用以下信号注释各测试用例内容与原始资料的对照情况:

- 🔵 **绿灯**: 参考原始资料几乎没有推测的情况
- 🟡 **黄灯**: 从原始资料进行合理推测的情况
- 🔴 **红灯**: 原始资料中没有的推测情况

## 质量判定标准

```
✅ 高质量:
- 测试执行: 成功(确认失败)
- 期望值: 明确具体
- 断言: 适当
- 实现方针: 明确
- 可靠性级别: 🔵(绿灯)较多

⚠️ 需改进:
- 无法执行测试
- 期望值模糊
- 实现方法不明
- 复杂的测试用例
- 可靠性级别: 🟡🔴(黄灯、红灯)较多
```

## TODO更新模式

```
- 将当前TODO"Red阶段(创建失败测试)"标记为"completed"
- 在TODO内容中反映失败测试创建阶段的完成
- 在TODO内容中记录质量判定结果
- 将下一阶段"Green阶段(最小实现)"添加到TODO
```

## 文件路径记载规则

- **使用以项目根目录为基准的相对路径**
- 不记载全路径(绝对路径)
- 例:
  - ❌ `/Users/username/projects/myapp/src/utils/helper.ts`
  - ✅ `src/utils/helper.ts`

## 🎯 UI开发任务的**E2E测试优先**方针

**在UI组件或屏幕功能的开发任务中,请最优先使用E2E测试(特别是Playwright):**

### 🚀 应**必须**使用E2E测试的情况

- **用户界面的动作确认** - UI组件的显示/隐藏、状态变更
- **画面跳转和导航的验证** - 页面间的移动、路由的动作
- **表单输入和提交功能的测试** - 输入验证、错误处理、提交后的动作
- **用户交互** - 点击、输入、滚动、拖放等
- **浏览器特定的动作确认** - 不同浏览器的兼容性测试
- **响应式设计的验证** - 根据屏幕大小的布局变更
- **无障碍功能的确认** - 键盘操作、屏幕阅读器支持
- **性能的确认** - 加载时间、渲染速度

### 🥇 **Playwright强烈推荐**框架

**UI任务原则上请使用Playwright:**

#### 1. **Playwright** (🎯最优先推荐)
   - ✅ 多浏览器支持(Chrome, Firefox, Safari)
   - ✅ 快速稳定的执行
   - ✅ 丰富的断言功能和选择器
   - ✅ 自动等待功能(自动等待元素的显示/隐藏)
   - ✅ 网络监控・模拟功能
   - ✅ 截图・视频录制
   - ✅ 性能测量功能
   - ✅ 无障碍测试集成

#### 2. **Cypress** (补充选项)
   - 开发者友好的API
   - 实时调试功能
   - ※ 有单标签限制

#### 3. **WebDriver/Selenium** (仅用于遗留系统对应)
   - 广泛的浏览器支持
   - 成熟的生态系统
   - ※ 配置复杂、执行速度慢

## 🧭 测试策略选择指南

### UI相关任务的情况 → **E2E测试最优先**
- 组件创建・修改
- 页面创建・布局变更
- 用户交互实现
- 表单功能实现
- 导航・路由

### 业务逻辑・API相关任务 → **单元・集成测试**
- 数据处理算法
- 验证函数
- API端点
- 数据库操作
- 工具函数

## 测试代码创建方针

### E2E测试重视的结构
- **采用Scenario-Given-When-Then模式**
- **场景定义**: 测试什么样的用户旅程
- **初始状态设置(Given)**: 页面访问、登录状态等
- **执行用户操作(When)**: 点击、输入、导航等
- **结果确认(Then)**: 屏幕显示、状态变化、页面跳转等

### 单元・集成测试用的结构
- **使用Given-When-Then模式**
- 测试数据的准备(Given)
- 实际处理的执行(When)
- 结果的验证(Then)

## 中文注释必需要求

测试代码必须包含以下中文注释:

### 测试用例开始时的注释

```javascript
describe('{{feature_name}}', () => {
  test('{{test_case_name}}', () => {
    // 【测试目的】: [用中文明确说明此测试要确认什么]
    // 【测试内容】: [说明具体测试什么样的处理]
    // 【期待的动作】: [说明正常动作时的结果]
    // 🔵🟡🔴 可靠性级别: [此测试的内容在多大程度上基于原始资料]

    // 【测试数据准备】: [为什么准备这个数据的理由]
    // 【初始条件设置】: [说明测试执行前的状态]
    const input = {{test_input}};

    // 【实际处理执行】: [说明调用哪个功能/方法]
    // 【处理内容】: [用中文说明执行的处理内容]
    const result = {{function_name}}(input);

    // 【结果验证】: [具体说明要验证什么]
    // 【期望值确认】: [说明期望的结果及其理由]
    expect(result).toBe({{expected_output}}); // 【确认内容】: [此验证要确认的具体项目] 🔵🟡🔴
  });
});
```

### 设置・清理的注释(根据需要)

```javascript
beforeEach(() => {
  // 【测试前准备】: [说明各测试执行前进行的准备工作]
  // 【环境初始化】: [将测试环境恢复到干净状态的理由和方法]
});

afterEach(() => {
  // 【测试后处理】: [说明各测试执行后进行的清理工作]
  // 【状态恢复】: [为不影响下一个测试而恢复状态的理由]
});
```

### 各expect语句的注释

各expect语句必须附上中文注释:

```javascript
expect(result.property).toBe(expectedValue); // 【确认内容】: [此验证要确认的具体项目和理由]
expect(result.array).toHaveLength(3); // 【确认内容】: [确认数组长度与期望值一致的理由]
expect(result.errors).toContain('error message'); // 【确认内容】: [确认包含特定错误消息的理由]
```

## TDD备忘录文件格式

`docs/implements/{需求名称}/{{task_id}}/{feature_name}-memo.md` 文件的格式:

```markdown
# TDD开发备忘录: {feature_name}

## 概要

- 功能名: [功能名]
- 开发开始: [日期时间]
- 当前阶段: [Red/Green/Refactor]

## 相关文件

- 原任务文件: `docs/tasks/{任务文件路径}.md`
- 需求定义: `docs/implements/{需求名称}/{{task_id}}/{feature_name}-requirements.md`
- 测试用例定义: `docs/implements/{需求名称}/{{task_id}}/{feature_name}-testcases.md`
- 实现文件: `[实现文件路径]`
- 测试文件: `[测试文件路径]`

## Red阶段(创建失败测试)

### 创建日期时间

[日期时间]

### 测试用例

[创建的测试用例概要]

### 测试代码

[实际的测试代码]

### 预期的失败

[期望什么样的失败]

### 下一阶段的要求事项

[Green阶段应实现的内容]

## Green阶段(最小实现)

### 实现日期时间

[日期时间]

### 实现方针

[最小实现的方针]

### 实现代码

[实际的实现代码]

### 测试结果

[测试通过的结果]

### 课题・改进点

[Refactor阶段应改进的点]

## Refactor阶段(质量改进)

### 重构日期时间

[日期时间]

### 改进内容

[具体的改进内容]

### 安全审查

[安全方面的确认结果]

### 性能审查

[性能方面的确认结果]

### 最终代码

[重构后的代码]

### 质量评价

[最终的质量评价]
```

# info

## 测试执行命令示例

### Playwright的情况
```bash
# 执行所有E2E测试
npx playwright test

# 执行特定测试文件
npx playwright test tests/e2e/{{feature_name}}.spec.js

# 非无头模式执行(显示浏览器)
npx playwright test --headed

# 在特定浏览器执行测试
npx playwright test --project=chromium
npx playwright test --project=firefox
npx playwright test --project=webkit

# 调试模式执行
npx playwright test --debug

# 生成报告
npx playwright show-report
```

### Cypress的情况
```bash
# 打开Cypress Test Runner
npx cypress open

# 无头模式执行测试
npx cypress run

# 执行特定测试文件
npx cypress run --spec "cypress/e2e/{{feature_name}}.cy.js"

# 在特定浏览器执行
npx cypress run --browser chrome
```

### 单元测试(Jest等)的情况
```bash
# 执行所有测试
npm test

# 执行特定测试文件
npm test {{feature_name}}.test.js

# 监视模式执行
npm test -- --watch

# 生成覆盖率报告
npm test -- --coverage
```

<red_phase_template>
您是TDD开发的Red阶段负责人。请根据以下信息创建失败的测试用例。

# Red阶段对象的信息

功能名: {{功能名}}
任务ID: {{任务ID}}
需求名称: {{需求名称}}
目标测试用例: {{目标测试用例名称}}(未指定的情况下为所有测试用例)
测试用例添加目标数: {{测试用例添加目标数}}
输出文件名: {{输出文件名}}

# 重要的注意事项

**所有文件路径请以项目根目录为基准用相对路径记载。**
**请勿使用绝对路径(/Users/... 或 C:\... 等)。**

例:
- ❌ `/Users/username/projects/myapp/src/utils/helper.ts`
- ✅ `src/utils/helper.ts`

# 测试创建步骤

请利用已读取的以下上下文信息:
- **任务笔记**(`note.md`)
  - 技术栈(使用技术・框架・测试框架)
  - 开发规则(编码规范・类型检查・测试要求)
  - 相关实现(现有的测试模式・参考代码)
  - 设计文档(数据模型・目录结构)
  - 注意事项(技术限制・安全要求・性能要求)
- **附加规则**(`docs/rule`, `docs/rule/tdd`, `docs/rule/tdd/red`)
- **现有的测试实现模式**(用symbol-searcher搜索的测试文件)
- **需求定义・测试用例定义**

基于这些信息,请创建满足以下要求的测试代码。

## 测试代码创建要求

- **使用语言/框架**: 基于读取的技术栈定义
- **测试必须在失败状态下创建**
- **测试名称用易于理解的中文描述**
- **明确记述断言(期望值的验证)**
- **以调用尚未实现的函数・方法的形式创建**
- **中文注释必需**(测试目的、内容、期待动作、可靠性级别)
- **使用Given-When-Then模式**(E2E的情况使用Scenario-Given-When-Then)

## 测试策略

### UI相关任务的情况 → **E2E测试(Playwright优先)**
- 组件创建・修改
- 页面创建・布局变更
- 用户交互实现
- 表单功能实现
- 导航・路由

### 业务逻辑・API相关任务 → **单元・集成测试**
- 数据处理算法
- 验证函数
- API端点
- 数据库操作
- 工具函数

## 单元测试・集成测试示例

```javascript
// 测试文件: {{test_file_name}}
describe('{{feature_name}}', () => {
  beforeEach(() => {
    // 【测试前准备】: 各测试执行前初始化测试环境,保证一致的测试条件
    // 【环境初始化】: 为了不受前一个测试的影响,将文件系统状态清除重置
  });

  afterEach(() => {
    // 【测试后处理】: 测试执行后删除创建的临时文件和目录
    // 【状态恢复】: 为不影响下一个测试,将系统恢复到原来的状态
  });

  test('{{test_case_name}}', () => {
    // 【测试目的】: {{test_purpose}}
    // 【测试内容】: {{test_description}}
    // 【期待的动作】: {{expected_behavior}}
    // 🔵🟡🔴 可靠性级别: [此测试的内容在多大程度上基于原始资料]

    // 【测试数据准备】: {{test_data_reason}}
    // 【初始条件设置】: {{initial_condition}}
    const input = {{test_input}};

    // 【实际处理执行】: {{function_description}}
    // 【处理内容】: {{process_description}}
    const result = {{function_name}}(input);

    // 【结果验证】: {{verification_description}}
    // 【期望值确认】: {{expected_result_reason}}
    expect(result).toBe({{expected_output}}); // 【确认内容】: {{specific_verification_point}}
  });
});
```

## UI任务用E2E测试示例(Playwright)

```javascript
// E2E测试文件: tests/e2e/{{feature_name}}.spec.js
import { test, expect } from '@playwright/test';

describe('{{feature_name}} E2E测试', () => {
  test.beforeEach(async ({ page }) => {
    // 【E2E测试前准备】: 启动浏览器,移动到测试对象页面
    // 【环境初始化】: 为独立执行各测试,重置页面状态
    await page.goto('/{{target_page}}');
  });

  test('{{ui_test_case_name}}', async ({ page }) => {
    // 【测试目的】: {{ui_test_purpose}}
    // 【测试内容】: {{ui_test_description}}
    // 【期待的动作】: {{expected_ui_behavior}}
    // 🔵🟡🔴 可靠性级别: [此测试的内容在多大程度上基于原始资料]

    // 【初始状态确认】: {{initial_ui_state_reason}}
    // 【画面显示确认】: {{screen_display_verification}}
    await expect(page.locator('{{initial_element_selector}}')).toBeVisible();
    // 【确认内容】: 确认初始状态下显示必要的元素

    // 【执行用户操作】: {{user_action_description}}
    // 【操作内容】: {{specific_action_description}}
    await page.click('{{target_button_selector}}');
    await page.fill('{{input_selector}}', '{{test_input_value}}');
    await page.click('{{submit_button_selector}}');

    // 【结果确认】: {{ui_result_verification}}
    // 【期待的显示变化】: {{expected_ui_changes}}
    await expect(page.locator('{{result_element_selector}}')).toContainText('{{expected_text}}');
    // 【确认内容】: {{specific_ui_verification_point}}

    // 【追加验证】: {{additional_verification_description}}
    await expect(page).toHaveURL('{{expected_url}}');
    // 【确认内容】: 确认跳转到正确的页面
  });

  test('{{responsive_test_case_name}}', async ({ page }) => {
    // 【测试目的】: 响应式设计的动作确认
    // 【测试内容】: 不同屏幕大小下UI显示和可用性的验证
    // 【期待的动作】: 在移动・平板・桌面尺寸下适当显示
    // 🔵🟡🔴 可靠性级别: [此测试的内容在多大程度上基于原始资料]

    // 【画面尺寸设置】: 移动尺寸的显示确认
    // 【响应式确认】: 测试小屏幕下的元素配置和可用性
    await page.setViewportSize({ width: 375, height: 667 });

    // 【移动显示确认】: 确认应用移动布局
    await expect(page.locator('{{mobile_navigation_selector}}')).toBeVisible();
    // 【确认内容】: 显示移动用导航

    // 【平板尺寸设置】: 中间尺寸的显示确认
    await page.setViewportSize({ width: 768, height: 1024 });

    // 【平板显示确认】: 确认平板布局的动作
    await expect(page.locator('{{tablet_layout_selector}}')).toBeVisible();
    // 【确认内容】: 适当显示平板用布局
  });
});
```

## 无障碍测试示例

```javascript
// 无障碍测试文件: tests/e2e/accessibility/{{feature_name}}.spec.js
import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

describe('{{feature_name}} 无障碍测试', () => {
  test('{{accessibility_test_case_name}}', async ({ page }) => {
    // 【测试目的】: Web无障碍指南(WCAG)合规性确认
    // 【测试内容】: 自动无障碍检查和键盘操作的验证
    // 【期待的动作】: 无无障碍违规,可用键盘操作
    // 🔵🟡🔴 可靠性级别: [此测试的内容在多大程度上基于原始资料]

    // 【页面加载】: 移动到测试对象页面
    await page.goto('/{{target_page}}');

    // 【自动无障碍检查】: 使用axe-core的自动检查
    // 【WCAG合规确认】: 确认颜色对比度、ALT文本、标签等
    const accessibilityScanResults = await new AxeBuilder({ page }).analyze();
    expect(accessibilityScanResults.violations).toEqual([]);
    // 【确认内容】: 确认未检测到无障碍违规

    // 【键盘操作确认】: Tab键焦点移动的验证
    // 【操作性确认】: 确认不使用鼠标即可使用所有功能
    await page.keyboard.press('Tab');
    await expect(page.locator('{{first_focusable_element}}')).toBeFocused();
    // 【确认内容】: 焦点移动到第一个可聚焦元素
  });
});
```

# 请提供

1. **测试代码**: 可执行形式,附带必需的中文注释
2. **测试执行命令**: 如何执行
3. **预期的失败消息**: 会出现什么样的错误
4. **实现后的期待**: 测试通过所需实现的概要

请务必使用 Write 工具将测试代码保存到 {{输出文件名}}。
之后,使用 Bash 工具执行测试,确认失败。
</red_phase_template>
